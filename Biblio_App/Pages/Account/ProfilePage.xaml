<?xml version="1.0" encoding="utf-8" ?>
<?xaml-comp compile="true" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Biblio_App.Pages.Account.ProfilePage"
             x:Name="Page">

    <Shell.TitleView>
        <Grid Padding="0,0,8,0" VerticalOptions="Center">
            <Label x:Name="TitleLabel" Text="Profiel" FontSize="22" FontAttributes="Bold" VerticalOptions="Center" TextColor="{StaticResource White}" />
        </Grid>
    </Shell.TitleView>

    <VerticalStackLayout Padding="20" Spacing="12">
        <Label x:Name="HeaderLabel" Text="Profiel" FontAttributes="Bold" FontSize="20" />

        <Frame Style="{StaticResource WelcomeCardStyle}">
            <VerticalStackLayout Spacing="8">
                <Label x:Name="EmailLabel" Text="E-mail" FontAttributes="Bold" />
                <Label Text="{Binding Email}" />
                <Label x:Name="FullNameLabel" Text="Volledige naam" FontAttributes="Bold" />
                <Entry x:Name="FullNameEntry" Text="{Binding FullName}" Placeholder="Voer volledige naam in" />
                <Label x:Name="FullNameError" TextColor="Red" FontSize="12" />
                <Label x:Name="RolesLabel" Text="Rollen" FontAttributes="Bold" />
                <Label Text="{Binding Roles}" />
                <HorizontalStackLayout Spacing="8" HorizontalOptions="Start">
                    <Button Text="Opslaan" Clicked="OnSaveProfileClicked" Style="{StaticResource PrimaryButton}" />
                    <Button Text="Wachtwoord wijzigen" Clicked="OnChangePasswordClicked" Style="{StaticResource SecondaryButton}" />
                    <Button Text="Logout" Clicked="OnLogoutClicked" Style="{StaticResource PrimaryButton}" />
                </HorizontalStackLayout>
                <Label x:Name="StatusLabel" TextColor="Gray" />
            </VerticalStackLayout>
        </Frame>
    </VerticalStackLayout>
</ContentPage>