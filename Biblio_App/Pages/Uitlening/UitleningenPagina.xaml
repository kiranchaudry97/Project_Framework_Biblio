<?xml version="1.0" encoding="utf-8" ?>
<?xaml-comp compile="true" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodel="clr-namespace:Biblio_App.ViewModels"
             xmlns:models="clr-namespace:Biblio_Models.Entiteiten;assembly=Biblio_Models"
             xmlns:converters="clr-namespace:Biblio_App.Converters"
             xmlns:local="clr-namespace:Biblio_App.Pages"
             x:Class="Biblio_App.Pages.UitleningenPagina"
             x:Name="PageRoot"
             x:DataType="viewmodel:UitleningenViewModel"
             Title="{Binding PageHeaderText}">

    <ContentPage.Resources>
        <ResourceDictionary>
            <!-- Local fallback so StaticResource resolves at compile time -->
            <Color x:Key="Gray700">#5A5A5A</Color>
            <SolidColorBrush x:Key="Gray700Brush" Color="{StaticResource Gray700}" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <Shell.TitleView>
        <Grid ColumnDefinitions="*,Auto" Padding="0,0,8,0" VerticalOptions="Center">
            <!-- named TitleLabel so code-behind can force-update the displayed title when language changes -->
            <Label x:Name="TitleLabel" Text="{Binding PageHeaderText}" FontSize="22" FontAttributes="Bold" VerticalOptions="Center" TextColor="{StaticResource White}" Grid.Column="0" />
            <Label x:Name="PageLanguageLabel"
                   Grid.Column="1"
                   WidthRequest="48"
                   Margin="0,0,12,0"
                   VerticalOptions="Center"
                   HorizontalOptions="End"
                   TextColor="{StaticResource White}"
                   BackgroundColor="Transparent"
                   HorizontalTextAlignment="End" />
        </Grid>
    </Shell.TitleView>

    <ScrollView x:Name="MainScroll">
        <VerticalStackLayout Padding="20" Spacing="12">
            <Label Text="{Binding PageHeaderText}" Style="{StaticResource PageHeaderLabel}" />

            <!-- Status / counts for debugging -->
            <HorizontalStackLayout Spacing="12" HorizontalOptions="Center">
                <Label Text="{Binding MembersLabel}" TextColor="{StaticResource White}" />
                <Label Text="{Binding LedenCount}" FontAttributes="Bold" TextColor="{StaticResource White}" />
                <Label Text="{Binding LoansLabel}" TextColor="{StaticResource White}" />
                <Label Text="{Binding UitleningenCount}" FontAttributes="Bold" TextColor="{StaticResource White}" />
                <Label Text="{Binding BooksLabel}" TextColor="{StaticResource White}" />
                <Label Text="{Binding BoekenCount}" FontAttributes="Bold" TextColor="{StaticResource White}" />
            </HorizontalStackLayout>

            <!-- DB path display and copy button -->
            <!--<HorizontalStackLayout Spacing="8" VerticalOptions="Center" HorizontalOptions="FillAndExpand">
                <Label Text="{Binding DbPathLabelText}" VerticalOptions="Center" TextColor="{StaticResource White}" />
                <Label x:Name="DbPathLabel" Text="{Binding DbPathNotLoadedText}" LineBreakMode="TailTruncation" HorizontalOptions="FillAndExpand" VerticalOptions="Center" TextColor="{StaticResource White}" />
                <Button Text="{Binding CopyPathButtonText}" Clicked="OnCopyDbPathClicked" />
            </HorizontalStackLayout>-->

            <Label Text="{Binding LastError}" TextColor="Crimson" HorizontalOptions="Center" IsVisible="{Binding LastError, Converter={StaticResource StringNotEmptyToBoolConverter}}" />

            <Frame Style="{StaticResource WelcomeCardStyle}">
                <VerticalStackLayout Spacing="12">
                    <!-- Filters (adapted from web design) - simplified: only search + filter button -->
                    <Grid ColumnDefinitions="*, Auto">
                        <Entry Grid.Column="0" Placeholder="Zoek op titel, auteur of ISBN" Text="{Binding SearchText}" BackgroundColor="Transparent" PlaceholderColor="#6E6E6E" />
                        <Button Grid.Column="1" Text="{Binding FilterButtonText}" Command="{Binding ZoekCommand}" BackgroundColor="{StaticResource Primary}" TextColor="White" CornerRadius="10" WidthRequest="88" HeightRequest="44" Margin="8,0,0,0" />
                    </Grid>

                    <VerticalStackLayout Spacing="8">
                        <HorizontalStackLayout Spacing="8" VerticalOptions="Center" HorizontalOptions="FillAndExpand">
                            <Picker ItemsSource="{Binding LedenList}" ItemDisplayBinding="{Binding FullName}" SelectedItem="{Binding FilterLid, Mode=TwoWay}" Title="Filter op lid" HorizontalOptions="StartAndExpand" />
                            <Picker ItemsSource="{Binding BoekenList}" ItemDisplayBinding="{Binding Titel}" SelectedItem="{Binding FilterBoek, Mode=TwoWay}" Title="Filter op boek" HorizontalOptions="StartAndExpand" />
                        </HorizontalStackLayout>

                        <HorizontalStackLayout Grid.Column="2" HorizontalOptions="EndAndExpand" Spacing="8" VerticalOptions="Center">
                            <!-- spacer column; content right-aligned in column 2 -->
                        </HorizontalStackLayout>

                        <HorizontalStackLayout Grid.Column="3" Spacing="8" VerticalOptions="Center" HorizontalOptions="End">
                           
                        </HorizontalStackLayout>
                    </VerticalStackLayout>

                    <!-- List of loans with item card matching screenshot -->
                    <CollectionView ItemsSource="{Binding Uitleningen}" SelectionMode="Single" SelectedItem="{Binding SelectedUitlening, Mode=TwoWay}">
                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="models:Lenen">
                                <Border Stroke="{StaticResource Gray100}" StrokeThickness="1" Padding="0" BackgroundColor="White" Margin="0,6">
                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="8" />
                                    </Border.StrokeShape>
                                    <!-- Simplified layout: left info, right actions -->
                                    <Grid Padding="12" ColumnDefinitions="*,Auto">
                                        <!-- Left: member and book info -->
                                        <VerticalStackLayout Grid.Column="0" Spacing="4" HorizontalOptions="StartAndExpand">
                                            <VerticalStackLayout.GestureRecognizers>
                                                <TapGestureRecognizer Tapped="OnItemHeaderTapped" />
                                            </VerticalStackLayout.GestureRecognizers>
                                            <Label FontAttributes="Bold" LineBreakMode="WordWrap" FontSize="14">
                                                <Label.FormattedText>
                                                    <FormattedString>
                                                        <Span Text="{Binding Lid.Voornaam}" />
                                                        <Span Text=" " />
                                                        <Span Text="{Binding Lid.AchterNaam}" />
                                                    </FormattedString>
                                                </Label.FormattedText>
                                            </Label>
                                            <Label Text="{Binding Boek.Titel}" TextColor="#5A5A5A" LineBreakMode="TailTruncation" FontSize="12" />

                                            <!-- show Start and Due dates in the item -->
                                            <VerticalStackLayout Spacing="2" HorizontalOptions="StartAndExpand">
                                                <Label FontSize="11" TextColor="#6E6E6E" HorizontalOptions="Start">
                                                    <Label.FormattedText>
                                                        <FormattedString>
                                                            <Span Text="{Binding BindingContext.StartLabel, Source={x:Reference PageRoot}}" />
                                                            <Span Text=": " />
                                                            <Span Text="{Binding StartDate, StringFormat='{0:dd-MM-yyyy}'}" />
                                                        </FormattedString>
                                                    </Label.FormattedText>
                                                </Label>
                                                <Label FontSize="11" TextColor="#6E6E6E" HorizontalOptions="Start">
                                                    <Label.FormattedText>
                                                        <FormattedString>
                                                            <Span Text="{Binding BindingContext.DueLabel, Source={x:Reference PageRoot}}" />
                                                            <Span Text=": " />
                                                            <Span Text="{Binding DueDate, StringFormat='{0:dd-MM-yyyy}'}" />
                                                        </FormattedString>
                                                    </Label.FormattedText>
                                                </Label>
                                            </VerticalStackLayout>
                                        </VerticalStackLayout>

                                        <!-- Right: action icons (keep details and delete) -->
                                        <VerticalStackLayout Grid.Column="1" HorizontalOptions="End" VerticalOptions="Center" WidthRequest="120" Spacing="6">
                                            <HorizontalStackLayout Spacing="6" VerticalOptions="Center" HorizontalOptions="End">
                                                <ImageButton Source="details_illustration.svg" WidthRequest="24" HeightRequest="24" BackgroundColor="Transparent" Clicked="OnViewLoanClicked" BindingContext="{Binding .}" />
                                                <ImageButton Source="delete_illustration.svg" WidthRequest="28" HeightRequest="28" BackgroundColor="Transparent" Clicked="OnDeleteLoanClicked" BindingContext="{Binding .}" />
                                            </HorizontalStackLayout>
                                            <!-- 'Mark returned' button removed as requested -->
                                        </VerticalStackLayout>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>

                    <!-- Edit / create form -->
                    <Border x:Name="EditForm" Stroke="{StaticResource Gray100}" StrokeThickness="1" Padding="10">
                        <VerticalStackLayout Spacing="8">
                            <!-- remove return/picker UI, keep simple fields -->
                            <Picker x:Name="BoekPicker" ItemsSource="{Binding BoekenList}" ItemDisplayBinding="{Binding Titel}" SelectedItem="{Binding SelectedBoek}" Title="{Binding BookTitle}" />

                            <Picker x:Name="LidPicker" ItemsSource="{Binding LedenList}" ItemDisplayBinding="{Binding FullName}" SelectedItem="{Binding SelectedLid}" Title="{Binding MemberTitle}" />

                            <Label Text="{Binding StartLabel}" TextColor="#5A5A5A" />
                            <DatePicker x:Name="StartDatePicker" Date="{Binding StartDate}" />

                            <Label Text="{Binding DueLabel}" TextColor="#5A5A5A" />
                            <DatePicker x:Name="DueDatePicker" Date="{Binding DueDate}" />

                            <HorizontalStackLayout Spacing="12" HorizontalOptions="End">
                                <!-- textual delete button for form -->
                                <Button Text="{Binding DeleteButtonText}" Command="{Binding VerwijderCommand}" Style="{StaticResource DangerButtonSmall}" />
                                <Button Text="{Binding SaveButtonText}" Command="{Binding OpslaanCommand}" Style="{StaticResource PrimaryButton}" />
                             </HorizontalStackLayout>
                         </VerticalStackLayout>
                     </Border>
                 </VerticalStackLayout>
             </Frame>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>