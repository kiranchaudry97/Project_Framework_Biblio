<?xml version="1.0" encoding="utf-8" ?>
<?xaml-comp compile="true" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodel="clr-namespace:Biblio_App.ViewModels"
             xmlns:models="clr-namespace:Biblio_Models.Entiteiten;assembly=Biblio_Models"
             xmlns:converters="clr-namespace:Biblio_App.Converters"
             xmlns:local="clr-namespace:Biblio_App.Pages"
             x:Class="Biblio_App.Pages.UitleningenPagina"
             x:Name="PageRoot"
             x:DataType="viewmodel:UitleningenViewModel"
             Title="{Binding PageHeaderText}">

    <ContentPage.Resources>
        <ResourceDictionary>
            <!-- Lokale fallback zodat StaticResource resolveert tijdens compilatie -->
            <Color x:Key="Gray700">#5A5A5A</Color>
            <SolidColorBrush x:Key="Gray700Brush" Color="{StaticResource Gray700}" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <Shell.TitleView>
        <Grid ColumnDefinitions="*,Auto" Padding="0,0,8,0" VerticalOptions="Center" BackgroundColor="{StaticResource Primary}">
            <!-- genaamd TitleLabel zodat code-behind de weergegeven titel geforceerd kan updaten wanneer de taal wijzigt -->
            <Label x:Name="TitleLabel" Text="{Binding PageHeaderText}" FontSize="22" FontAttributes="Bold" VerticalOptions="Center" TextColor="White" Grid.Column="0" />
            <Label x:Name="PageLanguageLabel"
                   Grid.Column="1"
                   WidthRequest="48"
                   Margin="0,0,12,0"
                   VerticalOptions="Center"
                   HorizontalOptions="End"
                   TextColor="{StaticResource White}"
                   BackgroundColor="Transparent"
                   HorizontalTextAlignment="End" />
        </Grid>
    </Shell.TitleView>

    <ScrollView x:Name="MainScroll" BackgroundColor="White">
        <VerticalStackLayout Padding="20" Spacing="12" BackgroundColor="White">
            <Label Text="{Binding PageHeaderText}" Style="{StaticResource PageHeaderLabel}" TextColor="#212121" />

            <!-- Status / aantallen met navy blue achtergrond zoals design -->
            <Frame Style="{StaticResource UitleningenHeader}" Padding="12,8">
                <HorizontalStackLayout Spacing="12" HorizontalOptions="Center">
                    <Label Text="{Binding MembersLabel}" Style="{StaticResource StatLabel}" />
                    <Label Text="{Binding LedenCount}" FontAttributes="Bold" TextColor="White" FontSize="14" />
                    <Label Text="{Binding LoansLabel}" Style="{StaticResource StatLabel}" />
                    <Label Text="{Binding UitleningenCount}" FontAttributes="Bold" TextColor="White" FontSize="14" />
                    <Label Text="{Binding BooksLabel}" Style="{StaticResource StatLabel}" />
                    <Label Text="{Binding BoekenCount}" FontAttributes="Bold" TextColor="White" FontSize="14" />
                </HorizontalStackLayout>
            </Frame>

            <!-- DB pad weergave en kopieer knop -->
            <!--<HorizontalStackLayout Spacing="8" VerticalOptions="Center" HorizontalOptions="FillAndExpand">
                <Label Text="{Binding DbPathLabelText}" VerticalOptions="Center" TextColor="{StaticResource White}" />
                <Label x:Name="DbPathLabel" Text="{Binding DbPathNotLoadedText}" LineBreakMode="TailTruncation" HorizontalOptions="FillAndExpand" VerticalOptions="Center" TextColor="{StaticResource White}" />
                <Button Text="{Binding CopyPathButtonText}" Clicked="OnCopyDbPathClicked" />
            </HorizontalStackLayout>-->

            <Label Text="{Binding LastError}" TextColor="Crimson" HorizontalOptions="Center" IsVisible="{Binding LastError, Converter={StaticResource StringNotEmptyToBoolConverter}}" />

            <Frame Style="{StaticResource WelcomeCardStyle}" BackgroundColor="White">
                <VerticalStackLayout Spacing="12">
                    <!-- Filters met styled components -->
                    <Grid ColumnDefinitions="*, Auto">
                        <Entry Grid.Column="0" 
                               Placeholder="Zoek op titel, auteur of ISBN" 
                               Text="{Binding SearchText}" 
                               Style="{StaticResource SearchEntry}"
                               BackgroundColor="#F5F5F5" />
                        <Button Grid.Column="1" 
                                Text="{Binding FilterButtonText}" 
                                Command="{Binding ZoekCommand}" 
                                Style="{StaticResource FilterButton}"
                                Margin="8,0,0,0" />
                    </Grid>

                    <VerticalStackLayout Spacing="8">
                        <HorizontalStackLayout Spacing="8" VerticalOptions="Center" HorizontalOptions="FillAndExpand">
                            <Picker ItemsSource="{Binding LedenList}" ItemDisplayBinding="{Binding FullName}" SelectedItem="{Binding FilterLid, Mode=TwoWay}" Title="Filter op lid" HorizontalOptions="StartAndExpand" BackgroundColor="#F5F5F5" TextColor="#212121" TitleColor="#6E6E6E" />
                            <Picker ItemsSource="{Binding BoekenList}" ItemDisplayBinding="{Binding Titel}" SelectedItem="{Binding FilterBoek, Mode=TwoWay}" Title="Filter op boek" HorizontalOptions="StartAndExpand" BackgroundColor="#F5F5F5" TextColor="#212121" TitleColor="#6E6E6E" />
                        </HorizontalStackLayout>

                        <HorizontalStackLayout Grid.Column="2" HorizontalOptions="EndAndExpand" Spacing="8" VerticalOptions="Center">
                            <!-- spacer kolom; inhoud rechts uitgelijnd in kolom 2 -->
                        </HorizontalStackLayout>

                        <HorizontalStackLayout Grid.Column="3" Spacing="8" VerticalOptions="Center" HorizontalOptions="End">
                           
                        </HorizontalStackLayout>
                    </VerticalStackLayout>

                    <!-- Lijst van uitleningen met item kaart die overeenkomt met screenshot -->
                    <CollectionView ItemsSource="{Binding Uitleningen}" SelectionMode="Single" SelectedItem="{Binding SelectedUitlening, Mode=TwoWay}">
                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="models:Lenen">
                                <Border Stroke="{StaticResource Gray100}" StrokeThickness="1" Padding="0" BackgroundColor="White" Margin="0,6">
                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="8" />
                                    </Border.StrokeShape>
                                    <!-- Vereenvoudigde lay-out: alleen info -->
                                    <Grid Padding="12" ColumnDefinitions="*">
                                        <!-- Lid en boek info -->
                                        <VerticalStackLayout Grid.Column="0" Spacing="6" HorizontalOptions="FillAndExpand">
                                            <VerticalStackLayout.GestureRecognizers>
                                                <TapGestureRecognizer Tapped="OnItemHeaderTapped" />
                                            </VerticalStackLayout.GestureRecognizers>
                                            
                                            <!-- Book Title - Most prominent -->
                                            <Label Text="{Binding Boek.Titel}" 
                                                   FontAttributes="Bold" 
                                                   FontSize="16" 
                                                   TextColor="{StaticResource Primary}"
                                                   LineBreakMode="WordWrap" />
                                            
                                            <!-- Lid Naam -->
                                            <HorizontalStackLayout Spacing="4">
                                                <Label Text="Lid:" FontSize="12" TextColor="#6E6E6E" FontAttributes="Bold" />
                                                <Label FontSize="12" TextColor="#212121">
                                                    <Label.FormattedText>
                                                        <FormattedString>
                                                            <Span Text="{Binding Lid.Voornaam}" />
                                                            <Span Text=" " />
                                                            <Span Text="{Binding Lid.AchterNaam}" />
                                                        </FormattedString>
                                                    </Label.FormattedText>
                                                </Label>
                                            </HorizontalStackLayout>

                                            <!-- toon Start en Einddatums in het item -->
                                            <HorizontalStackLayout Spacing="12">
                                                <HorizontalStackLayout Spacing="4">
                                                    <Label Text="Start:" 
                                                           FontSize="11" 
                                                           TextColor="#6E6E6E" 
                                                           FontAttributes="Bold" />
                                                    <Label Text="{Binding StartDate, StringFormat='{0:dd-MM-yyyy}'}" 
                                                           FontSize="11" 
                                                           TextColor="#212121" />
                                                </HorizontalStackLayout>
                                                
                                                <HorizontalStackLayout Spacing="4">
                                                    <Label Text="Tot:" 
                                                           FontSize="11" 
                                                           TextColor="#6E6E6E" 
                                                           FontAttributes="Bold" />
                                                    <Label Text="{Binding DueDate, StringFormat='{0:dd-MM-yyyy}'}" 
                                                           FontSize="11" 
                                                           TextColor="#212121" />
                                                </HorizontalStackLayout>
                                            </HorizontalStackLayout>
                                        </VerticalStackLayout>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>

                    <!-- Bewerk / creëer formulier -->
                    <Border x:Name="EditForm" Stroke="#E1E1E1" StrokeThickness="1" Padding="10" BackgroundColor="White">
                        <VerticalStackLayout Spacing="12">
                            <!-- Boek picker met expliciete styling -->
                            <Label Text="{Binding BookTitle}" TextColor="#5A5A5A" FontSize="14" FontAttributes="Bold" />
                            <Picker x:Name="BoekPicker" 
                                    ItemsSource="{Binding BoekenList}" 
                                    ItemDisplayBinding="{Binding Titel}" 
                                    SelectedItem="{Binding SelectedBoek}" 
                                    Title="Selecteer boek"
                                    BackgroundColor="#F5F5F5"
                                    TextColor="#212121"
                                    TitleColor="#6E6E6E"
                                    HeightRequest="45"
                                    FontSize="14" />
                            
                            <!-- Weergave geselecteerde boek details -->
                            <Frame Padding="8" BackgroundColor="#E8F5E9" IsVisible="{Binding SelectedBoek, Converter={StaticResource IsNotNullConverter}}">
                                <VerticalStackLayout Spacing="2">
                                    <Label Text="Geselecteerd boek:" FontSize="11" TextColor="#2E7D32" FontAttributes="Bold" />
                                    <Label Text="{Binding SelectedBoek.Titel}" FontSize="13" TextColor="#212121" FontAttributes="Bold" />
                                    <Label FontSize="11" TextColor="#5A5A5A">
                                        <Label.FormattedText>
                                            <FormattedString>
                                                <Span Text="Auteur: " />
                                                <Span Text="{Binding SelectedBoek.Auteur}" />
                                            </FormattedString>
                                        </Label.FormattedText>
                                    </Label>
                                    <Label FontSize="11" TextColor="#5A5A5A">
                                        <Label.FormattedText>
                                            <FormattedString>
                                                <Span Text="ISBN: " />
                                                <Span Text="{Binding SelectedBoek.Isbn}" />
                                            </FormattedString>
                                        </Label.FormattedText>
                                    </Label>
                                </VerticalStackLayout>
                            </Frame>

                            <!-- Lid picker met expliciete styling -->
                            <Label Text="{Binding MemberTitle}" TextColor="#5A5A5A" FontSize="14" FontAttributes="Bold" />
                            <Picker x:Name="LidPicker" 
                                    ItemsSource="{Binding LedenList}" 
                                    ItemDisplayBinding="{Binding FullName}" 
                                    SelectedItem="{Binding SelectedLid}" 
                                    Title="Selecteer lid"
                                    BackgroundColor="#F5F5F5"
                                    TextColor="#212121"
                                    TitleColor="#6E6E6E"
                                    HeightRequest="45"
                                    FontSize="14" />
                            
                            <!-- Weergave geselecteerde lid details -->
                            <Frame Padding="8" BackgroundColor="#E3F2FD" IsVisible="{Binding SelectedLid, Converter={StaticResource IsNotNullConverter}}">
                                <VerticalStackLayout Spacing="2">
                                    <Label Text="Geselecteerd lid:" FontSize="11" TextColor="#1565C0" FontAttributes="Bold" />
                                    <Label FontSize="13" TextColor="#212121" FontAttributes="Bold">
                                        <Label.FormattedText>
                                            <FormattedString>
                                                <Span Text="{Binding SelectedLid.Voornaam}" />
                                                <Span Text=" " />
                                                <Span Text="{Binding SelectedLid.AchterNaam}" />
                                            </FormattedString>
                                        </Label.FormattedText>
                                    </Label>
                                    <Label Text="{Binding SelectedLid.Email}" FontSize="11" TextColor="#5A5A5A" />
                                    <Label Text="{Binding SelectedLid.Telefoon}" FontSize="11" TextColor="#5A5A5A" IsVisible="{Binding SelectedLid.Telefoon, Converter={StaticResource StringNotEmptyToBoolConverter}}" />
                                </VerticalStackLayout>
                            </Frame>

                            <!-- Start datum met expliciete styling -->
                            <Label Text="{Binding StartLabel}" TextColor="#5A5A5A" FontSize="14" FontAttributes="Bold" />
                            <DatePicker x:Name="StartDatePicker" 
                                        Date="{Binding StartDate}"
                                        BackgroundColor="#F5F5F5"
                                        TextColor="#212121"
                                        HeightRequest="45"
                                        FontSize="14"
                                        Format="dd-MM-yyyy" />

                            <!-- Einddatum met expliciete styling -->
                            <Label Text="{Binding DueLabel}" TextColor="#5A5A5A" FontSize="14" FontAttributes="Bold" />
                            <DatePicker x:Name="DueDatePicker" 
                                        Date="{Binding DueDate}"
                                        BackgroundColor="#F5F5F5"
                                        TextColor="#212121"
                                        HeightRequest="45"
                                        FontSize="14"
                                        Format="dd-MM-yyyy" />

                            <!-- Knoppen met proper styles -->
                            <HorizontalStackLayout Spacing="12" HorizontalOptions="End" Margin="0,8,0,0">
                                <Button Text="{Binding DeleteButtonText}" 
                                        Command="{Binding VerwijderCommand}" 
                                        Style="{StaticResource DangerButton}"
                                        WidthRequest="100"
                                        HeightRequest="40" />
                                <Button Text="{Binding SaveButtonText}" 
                                        Command="{Binding OpslaanCommand}" 
                                        Style="{StaticResource PrimaryButton}"
                                        WidthRequest="100"
                                        HeightRequest="40" />
                             </HorizontalStackLayout>
                         </VerticalStackLayout>
                     </Border>
                 </VerticalStackLayout>
             </Frame>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>