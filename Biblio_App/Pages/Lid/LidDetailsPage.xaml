<?xml version="1.0" encoding="utf-8" ?>
<?xaml-comp compile="true" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:Biblio_Models.Entiteiten;assembly=Biblio_Models"
             x:Class="Biblio_App.Pages.LidDetailsPage"
             x:DataType="models:Lid"
             x:Name="Page"
             Title="{Binding PageHeaderText, Source={x:Reference Page}}">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="12">
            <Label Text="{Binding PageHeaderText, Source={x:Reference Page}}" FontSize="22" FontAttributes="Bold" HorizontalOptions="Center" TextColor="{StaticResource White}" />

            <Frame Style="{StaticResource WelcomeCardStyle}">
                <VerticalStackLayout Spacing="8">
                    <Label Text="{Binding Voornaam}" FontAttributes="Bold" FontSize="18" />
                    <Label Text="{Binding AchterNaam}" FontSize="14" />
                    <Label Text="{Binding Email}" FontSize="14" />

                    <HorizontalStackLayout HorizontalOptions="End" Spacing="12">
                        <Button Style="{StaticResource PrimaryButton}" Text="{Binding EditButtonText, Source={x:Reference Page}}" Command="{Binding BindingContext.ItemEditCommand, Source={x:Reference Page}}" CommandParameter="{Binding .}" WidthRequest="120" />
                        <Button Style="{StaticResource PrimaryButton}" Text="{Binding BackButtonText, Source={x:Reference Page}}" Clicked="OnBackClicked" WidthRequest="120" />
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </Frame>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>