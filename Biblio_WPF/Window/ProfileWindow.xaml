<!--
    commit bericht
--> 

<Page x:Class="Biblio_WPF.Window.ProfileWindow"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:Biblio_WPF.Window"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProfileWindow">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Profiel" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" />

        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,12,0,0" Width="420" HorizontalAlignment="Center">
            <StackPanel Orientation="Horizontal" Margin="0,6">
                <TextBlock Width="120" VerticalAlignment="Center">E-mail:</TextBlock>
                <TextBox x:Name="EmailBox" IsReadOnly="True" Width="280" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,6">
                <TextBlock Width="120" VerticalAlignment="Center">Volledige naam:</TextBlock>
                <TextBox x:Name="FullNameBox" Width="280" />
            </StackPanel>
            <TextBlock x:Name="FullNameError" Foreground="Red" FontSize="12" Margin="120,0,0,0" />

            <StackPanel Orientation="Horizontal" Margin="0,6">
                <TextBlock Width="120" VerticalAlignment="Center">Rol:</TextBlock>
                <TextBox x:Name="RoleBox" IsReadOnly="True" Width="280" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,6" HorizontalAlignment="Left">
                <Button x:Name="SaveProfileBtn" Content="Opslaan" Width="120" Height="36" Style="{StaticResource PrimaryButton}" Click="OnSaveProfile" />
                <Button x:Name="ChangePasswordBtn" Content="Wachtwoord wijzigen" Width="160" Height="36" Margin="8,0,0,0" Style="{StaticResource PrimaryButton}" Click="OnChangePassword" />
            </StackPanel>
        </StackPanel>

        <TextBlock Grid.Row="3" x:Name="StatusText" Foreground="Gray" Margin="0,12,0,0" HorizontalAlignment="Left" />
    </Grid>
</Page>
