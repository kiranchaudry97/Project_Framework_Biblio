<Page x:Class="Biblio_WPF.Window.LidWindow"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:Biblio_WPF.Window"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="1000"
      Title="LidWindow">

    <Grid Background="{DynamicResource BackgroundBrush}" Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" LastChildFill="True" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center">
                <Button Content="Terug" Click="OnBack" Margin="0,0,8,0"/>
                <TextBox x:Name="MemberSearchBox" Width="360" Margin="0,0,8,0" />
                <Button Content="Zoek" Click="OnSearch" Background="#0078D7" Foreground="White" Padding="8,4" Margin="0,0,8,0"/>
                <Button Content="Nieuw" Click="OnNewMember" Margin="0,0,8,0"/>
            </StackPanel>

            <TextBlock x:Name="MembersCountLabel" DockPanel.Dock="Right" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,8,0"/>
        </DockPanel>

        <DataGrid x:Name="MembersGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" SelectionMode="Single" SelectionChanged="MembersGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Voornaam" Binding="{Binding Voornaam}" Width="200"/>
                <DataGridTextColumn Header="Achternaam" Binding="{Binding AchterNaam}" Width="200"/>
                <DataGridTextColumn Header="E-mail" Binding="{Binding Email}" Width="*"/>
                <DataGridTextColumn Header="Telefoon" Binding="{Binding Telefoon}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="2" BorderBrush="#DDD" BorderThickness="1" CornerRadius="4" Padding="12" Margin="0,8,0,0">
            <!-- Responsive grid: star-sized input columns, Auto column for buttons so they stay visible -->
            <Grid VerticalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.2*" />
                    <ColumnDefinition Width="1.2*" />
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Labels -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Voornaam" Margin="0,0,12,6" FontWeight="SemiBold" />
                <TextBlock Grid.Row="0" Grid.Column="1" Text="Achternaam" Margin="0,0,12,6" FontWeight="SemiBold" />
                <TextBlock Grid.Row="0" Grid.Column="2" Text="E-mail" Margin="0,0,12,6" FontWeight="SemiBold" />
                <TextBlock Grid.Row="0" Grid.Column="3" Text="Telefoon" Margin="0,0,12,6" FontWeight="SemiBold" />

                <!-- Inputs -->
                <TextBox Grid.Row="1" Grid.Column="0" x:Name="VoornaamBox" Height="24" Margin="0,0,12,0" VerticalAlignment="Center" MinWidth="80" />
                <TextBox Grid.Row="1" Grid.Column="1" x:Name="AchternaamBox" Height="24" Margin="0,0,12,0" VerticalAlignment="Center" MinWidth="80" />
                <TextBox Grid.Row="1" Grid.Column="2" x:Name="EmailBox" Height="24" Margin="0,0,12,0" VerticalAlignment="Center" MinWidth="120" />
                <TextBox Grid.Row="1" Grid.Column="3" x:Name="TelefoonBox" Height="24" Margin="0,0,12,0" VerticalAlignment="Center" MinWidth="60" />

                <!-- Buttons in Auto column so they remain visible; they will wrap if window too narrow -->
                <StackPanel Grid.Row="1" Grid.Column="4" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Content="Opslaan" Click="OnSaveMember" Background="#0078D7" Foreground="White" Padding="8,4" Margin="0,0,8,0" MinWidth="80"/>
                    <Button Content="Verwijderen" Click="OnDeleteMember" Background="#D32F2F" Foreground="White" Padding="8,4" MinWidth="80"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>
